[build-system]
requires = ['setuptools>=61.0']
build-backend = 'setuptools.build_meta'

[project]
name = 'anjo-core'
version = '0.1.0'
description = 'GenAI-Driven Zero-Trust Autonomous Validation Framework (The Book of Genesis)'
requires-python = '>=3.10'
dependencies = [
    'networkx>=3.0',
    'pyyaml>=6.0',
    'playwright>=1.40.0'
]